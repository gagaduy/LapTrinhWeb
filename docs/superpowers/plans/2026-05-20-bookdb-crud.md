# BookDB CRUD Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the current Grade/Student demo with a BookDB MVC CRUD project that matches the provided ASM5 sample closely while using the root `DESIGN.md` to elevate the UI.

**Architecture:** Keep the app as a standard ASP.NET Core MVC app with EF Core + SQL Server. Replace the domain with `Book` and `Category`, seed sample data and local images, and rebuild the UI around a two-column catalog with a polished detail page and CRUD forms.

**Tech Stack:** ASP.NET Core MVC, Razor Views, EF Core 8, SQL Server, Docker SQL Server, custom CSS on top of Bootstrap.

---

### Task 1: Replace the old domain and database shape

**Files:**
- Create: `Models/Book.cs`
- Create: `Models/Category.cs`
- Modify: `Models/ApplicationDbContext.cs`
- Modify: `Program.cs`
- Delete: `Models/Grade.cs`
- Delete: `Models/Student.cs`
- Delete: `Controllers/GradesController.cs`
- Delete: `Views/Grades/Index.cshtml`
- Delete: `Migrations/*.cs`

- [ ] Define `Book` and `Category` with only the sample fields.
- [ ] Rewire `ApplicationDbContext` to expose `Books` and `Categories`.
- [ ] Remove old grade/student MVC files and stale migrations.
- [ ] Prepare the app to boot against the new schema.

### Task 2: Rebuild data and assets for BookDB

**Files:**
- Create: `Migrations/<new files generated by EF>`
- Create: `wwwroot/images/books/<sample image files>`
- Modify: `appsettings.json` only if needed for local SQL settings

- [ ] Generate a fresh migration for `Book` and `Category`.
- [ ] Seed categories and books in a migration so the sample content appears immediately.
- [ ] Add local sample cover images under `wwwroot/images/books`.

### Task 3: Build catalog, detail, and CRUD flows

**Files:**
- Create: `Controllers/BooksController.cs`
- Create: `Views/Books/Index.cshtml`
- Create: `Views/Books/Details.cshtml`
- Create: `Views/Books/Create.cshtml`
- Create: `Views/Books/Edit.cshtml`
- Create: `Views/Books/Delete.cshtml`

- [ ] Build the list page with category counts and book cards.
- [ ] Build the book detail page matching the sample layout more closely.
- [ ] Build create/edit/delete flows with server-side validation.

### Task 4: Replace layout and styling

**Files:**
- Modify: `Views/Shared/_Layout.cshtml`
- Modify: `Views/Home/Index.cshtml`
- Modify: `Views/Shared/_Layout.cshtml.css`
- Modify: `wwwroot/css/site.css`
- Modify: `Views/_ViewImports.cshtml` only if helpers/models require it
- Delete: old unused home content if no longer relevant

- [ ] Turn `Home/Index` into a redirect or entrypoint to the books catalog.
- [ ] Replace the old layout/navigation with the book-store structure from the sample.
- [ ] Apply `DESIGN.md` cues with warm canvas, strong display headings, colorful cards, dark CTA buttons, and cleaner detail composition.

### Task 5: Verify the rebuilt project

**Files:**
- Verify only

- [ ] Run `dotnet build` and fix any compile issues.
- [ ] Run `dotnet ef database update` against the configured SQL Server container.
- [ ] Run the app and verify list, detail, create, edit, and delete routes.

